import * as operations from "./ops";
import * as gens from "./gens";

const { KEYS, ...generals } = gens;

export const invasionOps = operations;
export const INVASION = generals;
export const IVK = KEYS;
