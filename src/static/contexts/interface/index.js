import * as operations from "./ops";
import * as gens from "./gens";

const { KEYS, ...generals } = gens;

export const interfaceOps = operations;
export const INTERFACE = generals;
export const ITK = KEYS;
